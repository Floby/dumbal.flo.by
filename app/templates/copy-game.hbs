{{screen-title title="Copier une partie" navigation="back"}}
{{outlet}}

<div class="copy-game">
  <div class="scan-preview">
    {{#unless errorMessage}}
      <CodeScanner
         @device={{selectedCam}}
         @onScanSuccess={{action "parseGame"}}
         @onDevicesError={{action "deviceError"}}
         @onDevicesFound={{action (mut cams)}}
      />
    {{else}}
    <div class="error">
      <p>
        {{fa-icon 'video-slash'}}<br>
        La caméra elle marche pas :(
      </p>
      <div class="detail">
       {{errorMessage}}
      </div>
    </div>
    {{/unless}}
  </div>
  <p class="help">
    Depuis une partie ouverte sur une autre téléphone, clique sur <strong>Copier</strong> et scanne le code qui
    apparaît avec ton tel.
  </p>
</div>
